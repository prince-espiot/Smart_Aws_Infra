#you can find the later tag here https://quay.io/repository/argoproj/argocd?tab=tags

---
# global:
#   image:
#     tag: "v2.13.3"

server:
  extraArgs:
    - --insecure

config:
  params:
    server.insecure: true